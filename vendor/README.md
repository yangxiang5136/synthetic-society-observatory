# Vendor Assets

## `three.min.js`

Three.js r124 classic browser build, vendored so the prototype works from a local `file://` URL without requiring a dev server.

License: MIT. See the license header in `three.min.js`.
